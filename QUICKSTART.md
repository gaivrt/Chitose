# 🚀 Chitose 快速开始

> **最新更新**: 已集成 LiveKit 自动口型同步功能！

## 📝 30 秒概览

Chitose 现在支持完整的 AI VTuber 工作流：

```
你说话 → Agent 识别 + 思考 + 回复 → Live2D 形象 + 自动口型同步
```

## ⚡ 5 分钟上手

### 步骤 1: 安装依赖

```bash
pip install -e .
```

### 步骤 2: 配置密钥

```bash
cp .env.example .env
vim .env  # 填入你的 API 密钥
```

需要的密钥（免费试用或付费）：
- LiveKit: https://cloud.livekit.io/
- OpenAI (或兼容 API): https://platform.openai.com/
- ElevenLabs: https://elevenlabs.io/
- Deepgram: https://deepgram.com/

### 步骤 3: 下载 Live2D SDK

1. 访问: https://www.live2d.com/en/download/cubism-sdk/download-web/
2. 下载 Cubism SDK for Web
3. 解压并复制 `Core/live2dcubismcore.min.js` 到 `web/lib/`

### 步骤 4: 准备 Live2D 模型

将你的 Cubism 4 模型放到 `models/` 目录，例如：

```
models/
└── 你的模型名/
    ├── 模型.model3.json
    ├── 模型.moc3
    └── textures/
```

然后编辑 `web/app.js` 中的模型路径：

```javascript
const CONFIG = {
    modelPath: '/models/你的模型名/模型.model3.json',  // 使用绝对路径
    // ...
};
```

### 步骤 5: 验证配置

```bash
python check_setup.py
```

看到 ✅ 就表示配置正确！

### 步骤 6: 启动系统

**终端 1** - Web 服务器:
```bash
python server.py
```

**终端 2** - Agent:
```bash
python main.py dev
```

**浏览器** - 访问:
```
http://localhost:8080
```

## 🎤 开始对话

在 LiveKit Playground (https://cloud.livekit.io/):
1. 进入你的项目
2. 点击 "Playground"
3. 连接到房间 `test-room`
4. 对着麦克风说话！

或者直接在聊天框输入文字。

## 🎭 效果预览

网页会自动显示 Live2D 模型，当 Agent 说话时：
- ✅ 音频实时播放
- ✅ 嘴巴自动同步
- ✅ 延迟 < 500ms

## 🔧 故障排除

### ❌ 依赖缺失
```bash
pip install -e .
```

### ❌ 模型加载失败
检查:
1. `web/lib/live2dcubismcore.min.js` 是否存在
2. 模型路径是否正确
3. 是否通过 HTTP 服务器访问（不能直接打开 HTML）

### ❌ LiveKit 连接失败
检查 `.env` 中的 LiveKit 配置是否正确。

### ❌ 没有声音
1. 确保 Agent 正在运行
2. 点击页面允许音频播放

### ❌ 口型不同步
调整 `web/app.js` 中的参数：

```javascript
const CONFIG = {
    // ...
    mouthSensitivityExponent: 0.5,  // 试试 0.3-0.7
    mouthAmplificationFactor: 1.2,  // 试试 0.8-1.5
};
```

## 📚 详细文档

- [完整使用指南](docs/USAGE_GUIDE.md)
- [技术架构](docs/TECHNICAL_REVIEW.md)
- [开发日志](docs/DEV_LOG.md)
- [Web 端说明](web/README.md)

## 🎯 下一步

恭喜！你已经完成了基础设置。接下来可以：

1. **自定义人设**: 编辑 `config/default.yaml`
2. **更换声音**: 在 ElevenLabs 选择喜欢的声音
3. **优化口型**: 调整 web/app.js 的参数
4. **接入 B 站弹幕**: 查看 TODO.md 的后续计划

## 💡 提示

- 使用 `空格 + 拖动` 移动模型位置
- 使用 `Ctrl + 滚轮` 缩放模型大小
- 打开浏览器控制台（F12）查看详细日志

## 🆘 需要帮助？

运行系统检查：
```bash
python check_setup.py
```

查看完整文档：
```bash
cat docs/USAGE_GUIDE.md
```

---

🌸 **享受你的 AI VTuber 之旅！**
